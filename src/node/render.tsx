export { default } from '@adapters/express/render';

export type {
  IRequestContext,
  IRenderOptions,
  IRenderParams,
  IRouterReadyOut,
  IShellReadyOut,
  TRender,
} from '@adapters/express/render';
